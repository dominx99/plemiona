{% extends "layouts/game.twig" %}

{% block title %}{{ building.name }}{% endblock title %}

{% block content %}
    <div class="alert alert-secondary">
        <p>
            Ulepszanie <span class="{{ building.color }}">{{ building.name }}</span> pozwala na rekrutację jednostek (jeszcze nie zaimplementowane)
        </p>

        <div>
            {{ building | json_encode }}
        </div>
    </div>
{% endblock content %}
